import random

# TODO: Рекомендуется обернуть весь код в функцию main() для лучшей структуры
# TODO: Добавить if __name__ == '__main__': main() в конце файла
print('Добро пожаловать в игру "Угадай загадку"!')

# TODO: Названия констант хорошо написаны БОЛЬШИМИ_БУКВАМИ, но лучше объединить
# загадки и ответы в одну структуру данных, например:
# RIDDLES_DATA = [
#     {'question': 'Каких камней нет в море?', 'answer': 'Сухих'},
#     {'question': 'Что нельзя съесть на завтрак?', 'answer': 'Обед и ужин'},
#     ...
# ]
RIDDLES = ['Каких камней нет в море?', 'Что нельзя съесть на завтрак?',
'У вас есть только одна спичка. В тёмной комнате стоят керосиновая лампа, печь и свеча. Что вы зажжёте в первую очередь?',
'Может ли страус назвать себя птицей?' ]
ANSWERS = ['Сухих', 'Обед и ужин', 'Спичку', 'Нет']

# ОШИБКА! randint(0,4) может вернуть 4, но максимальный индекс списка = 3 (IndexError)
# ИСПРАВИТЬ: key = random.randint(0, len(RIDDLES) - 1)
# ИЛИ ЛУЧШЕ: key = random.randrange(len(RIDDLES))
key = random.randint(0,4)

# TODO: Вынести количество попыток в константу MAX_ATTEMPTS для гибкости
attempts = 3

print('Загадка: ', RIDDLES[key])

# TODO: Можно использовать более питонический подход: for _ in range(attempts)
# TODO: Добавить обработку регистра: answer.strip().lower() == ANSWERS[key].lower()
while attempts != 0:
    answer = input('Ваш ответ: ')
    if answer == ANSWERS[key]:
       # TODO: Можно добавить сообщение о количестве использованных попыток
       print('Верно! Молодец!')
       break 
    else:
       attempts -= 1
       # TODO: Добавить проверку на attempts == 0 и вывести правильный ответ
       # TODO: Можно использовать f-строки: f'Неверно! Осталось попыток: {attempts}'
       print('Неверно! Осталось попыток: ', attempts)

# TODO: Добавить сообщение после цикла, если закончились попытки:
# if attempts == 0:
#     print(f'Игра окончена! Правильный ответ: {ANSWERS[key]}')
